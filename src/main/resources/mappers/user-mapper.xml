<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.multitable.dao.UserDAO">
    <resultMap id="UserMap" type="com.example.multitable.model.User">
        <id property="id" column="id" jdbcType="LONG"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>

        <association property="company" javaType="com.example.multitable.model.Company" column="company_id">
            <id property="id" column="companyid" jdbcType="LONG"/>
            <result property="companyName" column="companyname" jdbcType="VARCHAR"/>
        </association>

        <collection property="accounts" ofType="com.example.multitable.model.Account" column="user_id">
            <id property="id" column="accountid" javaType="LONG"/>
            <result property="accountName" column="accountname" jdbcType="VARCHAR"/>
        </collection>
    </resultMap>
    <select id="getAllUsers" resultMap="UserMap">
        select u.id, u.name, c.id companyid, c.name companyname, a.id accountid, a.name accountname
        from user u left join company c on u.company_id = c.id
        left join account a on u.id = a.user_id
    </select>
    <select id="getUserWithAccount" resultType="com.example.multitable.model.User"></select>
    <select id="getUserWithCompany" resultType="com.example.multitable.model.User"></select>
</mapper>